#include "monty.h"


/**
 * nop - does nothing
 *
 * @stack: pointer to the top of the stack (unused)
 * @line_number: line number where the command was called (unused)
 *
 * Return: nothing
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
